>permutations.pl
sub getAllPermutations {
  
}

sub getPermutationOf {
  my $replaceCodon = shift @_;
  my @possibilities = split //, shift @_;
  my @primers = @_;
  my @results;
  
  for each my $primer ( @primers )
  {
    while( 
  }
}
